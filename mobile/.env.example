# URL del backend (Fastify) sin ruta /api — el cliente usa /api/v1/...
# En el móvil real (Capacitor), usa la IP de tu PC o el dominio del API.
VITE_API_URL=http://localhost:3333

# Opcional: "true" = inicia con modo demostración (datos de ejemplo, sin API). En login también: "Ver app en modo demostración".
# VITE_DEV_BYPASS_AUTH=true
